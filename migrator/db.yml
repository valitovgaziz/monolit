development:
  driver: postgres
  source: dbname=postgres host=localhost port=5432 user=postgres password=postgres sslmode=disable
  charset: UTF8
  
test:
  driver: postgres
  source: dbname=postgres host=localhost port=5432 user=postgres password=postgres sslmode=disable
  charset: UTF8
  
production:
  driver: postgres
  source: dbname=postgres host=localhost port=5432 user=postgres password=postgres sslmode=disable
  charset: UTF8